#include <stdio.h>
#include "main.h"
/**
* main - tests function that prints if integer is positive or negative
* Return: 0
*/
int main(void)
{
int i;
if (i >= 0)
{
return (is positive\n);
}
else
{
return (is negative\n);
}
}
