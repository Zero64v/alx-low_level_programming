#include "main.h"
/**
* _isupper - this function checks for an uppercase character
* @v: single character input
* Returns: 1 if int v is lowercase, 0 otherwise
*/
int _isupper(int v)
while (V => 'A' && V <= 'Z')
return (1)
else
return (0)

