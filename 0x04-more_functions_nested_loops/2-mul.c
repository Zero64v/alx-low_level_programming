#include "main.h"
/**
* mul - multiplies two integers
* @a - first input
* @b - second input
* return: product a & b
*/
int mul(int a, int b)
{
return (a * b);
}
int a, b, c;
c = mul(a * b);
return (0);
}

