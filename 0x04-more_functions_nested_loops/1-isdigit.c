#include "main.h"
/**
* _isdigit - function checks for digit 0-9
* @c - checks function input
* Return: 1 if int c is digit, otherwise 0
*/
int _isdigit(void);
int _isdigit(int c)
{
if (c >= '0' && c <= '9')
return (1);
else
return (0)
}

